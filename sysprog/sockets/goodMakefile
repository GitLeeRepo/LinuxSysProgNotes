TARGET1=unix_socket_srv
TARGET2=unix_socket_clnt
CFILE1=$(TARGET1).c
CFILE2=$(TARGET2).c
CC=gcc
EXE=$(TARGET1) $(TARGET2)

all: info $(TARGET1) $(TARGET2)

info:
	@echo "Make file for: $(EXE)"

$(TARGET1): $(CFILE1)
	$(CC) $? -o $@

$(TARGET2): $(CFILE2)
	$(CC) $? -o $@

clean: 
	${RM} $(EXE) *.o

